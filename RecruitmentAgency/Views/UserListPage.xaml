<Page x:Class="RecruitmentAgency.Views.UserListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RecruitmentAgency.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" 
      Loaded="Page_Loaded"
      d:DesignWidth="800"
      Title="Список кадров">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition/>
            <RowDefinition Height="0.2*"/>
        </Grid.RowDefinitions>

        <StackPanel HorizontalAlignment="Left">
            <Button x:Name="BackBtn"
                    Width="120"
                    Height="35"
                    Margin="10 10"
                    Content="Назад"
                    Click="BackBtn_Click"/>
        </StackPanel>
        

        <StackPanel Grid.Row="1">
            <DataGrid x:Name="UsersPanel"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn  Width="*" Header="Имя" Binding="{Binding User.Name}"/>
                    <DataGridTextColumn  Width="*" Header="Фамилия" Binding="{Binding User.Surname}"/>
                    <DataGridTextColumn  Width="*" Header="Отчество" Binding="{Binding User.Patronymic}"/>
                    <DataGridTextColumn  Width="*" Header="Возраст" Binding="{Binding User.Age, StringFormat={}{0:dd.MM.yyyy}}"/>
                    <DataGridTextColumn  Width="*" Header="Желаемая работа" Binding="{Binding User.DesiredJob}"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel x:Name="MorePanel"
                    Grid.Row="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
            <Button x:Name="MoreBtn"
                    Width="120"
                    Height="30"
                    Content="Подробнее"
                    Click="MoreBtn_Click"/>
            <Button x:Name="AcceptBtn"
                    Width="120"
                    Height="30"
                    Margin="10 0 0 0"
                    Content="Принять на работу"
                    Click="AcceptBtn_Click"/>
            
        </StackPanel>
    </Grid>
</Page>
